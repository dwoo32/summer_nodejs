<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시글 확인 페이지</title>
</head>
<body>
    <h1>게시글 확인 및 수정/삭제 페이지</h1>

    <!-- 수정버튼을 클릭하면 수정폼 태그 내 수정데이터가 action요소의 지정된 url주소인 /article/modify 주소로 
    methodpost 방식으로 서버로 전송된다. 그러면 서버에서는 해당 주소 라우팅 매소드와 클라이언트 요청방식인 post방식으로
    맵핑되는 라우팅 매소드그ㅏ 구현되어 있어야 정보처리가 가능하다 -->

    <form method="post" action="/article/modify/<%=article.article_id%>" onsubmit="return checkForm();">
        제목: <input type="text" id="title" name="title" value="<%=article.title%>"/><br/>
        내용:<br/>
        <textarea id="contents" name="contents" rows="20" style="width: 500px;"><%=article.contents%></textarea><br/>
        게시여부:
        <select id="display" name="display">
            <option value="9">선택</option>
            <option value="1">게시</option>
            <option value="0">게시안함</option>
        </select>
        <br/>
        <input type="submit" value="수정"/>
        <input type="button" value="삭제" onClick="fnDelete();"/>
        <input type="button" value="목록이동" onClick="fnMoveList();"/>
    </form>

    <form method="post" action="/article/delete" id="deleteForm"> 
        <!-- method delete -->
        <input type="hidden" name="articel_id" value="<%=article.article_id%>"/>
    </form>   

    <script>
        function checkForm(){

            if(document.getElementById('title').value == ''){
                alert('제목을 입력하세요.');
                document.getElementById('title').focus();
                return false; //진행상황을 멈춰 ~ ! 
            }
            return true; //서버로 폼 데이터 전송 ! 
        }

        //일반 클라이언트 페이지에서는 get방식말고 form 방식을 하는 게 보안상 굿
        function fnDelete(){
            if(confirm('정말 삭제하시겠습니까?')){
                // location.href = '/article/delete?id=<%=article.article_id%>';
                document.getElementById('deleteForm').submit(); //=> form 방식
            }
        }

        function fnMoveList(){
            location.href = '/article/list';
        }


    </script>

</body>
</html>